services:
  - type: web
    name: laravel-backend
    env: php
    buildCommand: "composer install && php artisan key:generate && php artisan config:cache && php artisan migrate --force"
    startCommand: "php artisan serve --host 0.0.0.0 --port 10000"
    autoDeploy: true
